function sortBooks(books) {
    // Sort the books alphabetically and return the updated list

    books.sort();
    return books;
}

let result = sortBooks(["Math","English","Science","History"])

console.log(result);
